# -------------------------------------
# Radar server main configuration file.
# -------------------------------------

# If you're using Radar's defaults values for ALL options then you
# only need to specify the Radar server listen address.

listen:
    address: {:}
    port: {:}

run as:
    user: {:}
    group: {:}

log:
    to: {:}
    size: {:}
    rotations: {:}

pid file: {:}
polling time: {:}
checks: {:}
contacts: {:}
monitors: {:}
plugins: {:}

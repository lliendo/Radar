    connect:
        to: 127.0.0.1
        port: 3333

    run as:
        user: lucas
        group: lucas

    enforce ownership: False
    reconnect: False
    log file: /tmp/radar-client.log
    pidfile: /tmp/radar-client.pid
    checks: /usr/local/radar/client/checks

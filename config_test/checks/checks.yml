- check group:
    name: Basic health
    checks:
        - check:
            name: Load average
            path: /tmp/load_average.py
            args: -c a

        - check:
            name: Free RAM
            path: /tmp/free_ram.py
            args: -c a

- check group:
    name: MySQL health
    checks:
        - check:
            name: Load average
            path: /tmp/load_average.py
            args: -c a

        - check:
            name: Free RAM
            path: /tmp/free_ram.py
            args: -c a
